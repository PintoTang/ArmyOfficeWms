using System.Windows;
using System.Windows.Controls;

namespace CLDC.CLWS.CLWCS.WareHouse.Manage.View
{
    /// <summary>
    /// UCOrderAdd.xaml 的交互逻辑
    /// </summary>
    public partial class OrderAddView : Window
    {
        public OrderAddView()
        {
            InitializeComponent();    
            this.MouseLeftButtonDown += delegate { DragMove(); }; //拖拽
            
        }

        void OnClose_Click(object sender, RoutedEventArgs e)
        {
            this.Close();
        }

        private void CbStartAddr_OnLostFocus(object sender, RoutedEventArgs e)
        {
            ComboBox cb=sender as ComboBox;
            

        }
    }
}
