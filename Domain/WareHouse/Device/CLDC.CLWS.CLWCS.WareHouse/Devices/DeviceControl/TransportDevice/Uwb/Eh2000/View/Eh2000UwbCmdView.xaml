<UserControl x:Class="CLDC.CLWS.CLWCS.WareHouse.Device.Devices.DeviceControl.TransportDevice.Uwb.Eh2000.View.Eh2000UwbCmdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             
             d:DesignHeight="300" d:DesignWidth="450" Background="White" x:Name="_this">
    <Grid>
        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3">
            <Grid>
                <TextBox Style="{StaticResource MaterialDesignOutlinedTextFieldTextBox}" BorderThickness="0"
                                 VerticalAlignment="Top"  AcceptsReturn="True" TextWrapping="Wrap"
                                 materialDesign:HintAssist.Hint="输入参数" Width="{Binding ElementName=_this,Path=ActualWidth}"  Height="{Binding ElementName=_this,Path=ActualHeight}"
                                 VerticalScrollBarVisibility="Auto" IsEnabled="True" Text="{Binding RequesValue}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
