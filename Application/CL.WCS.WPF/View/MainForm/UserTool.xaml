<UserControl x:Class="CL.WCS.WPF.UserCtrl.MainForm.UserTool"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CL.WCS.WPF.UserCtrl.MainForm"
             mc:Ignorable="d" 
             
             d:DesignHeight="800" d:DesignWidth="80">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="1">
            <GridSplitter Height="5"></GridSplitter>
            <Button Width="50" Height="50"></Button>
            <GridSplitter Height="5"></GridSplitter>
            <Button Width="50" Height="50"></Button>
            <GridSplitter Height="5"></GridSplitter>
            <Button Width="50" Height="50"></Button>
            <GridSplitter Height="5"></GridSplitter>
        </StackPanel>
    </Grid>
</UserControl>
